previewsEnabled: true

services:
  - type: web
    name: poc
    env: docker
    pullRequestPreviewsEnabled: true
    envVars:
      - key: DB_URL
        fromDatabase:
          name: pocdb
          property: connectionString
      - key: TEST
        value: 999

databases:
  - name: pocdb
